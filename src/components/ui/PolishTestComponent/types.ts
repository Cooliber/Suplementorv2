// types.ts
export interface PolishTestComponentProps {
	className?: string;
	children?: React.ReactNode;
	// Add more props as needed
}

export interface PolishTestComponentData {
	id: string;
	name: string;
	// Add more data properties as needed
}
