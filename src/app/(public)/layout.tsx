/**
 * Public Layout
 * Layout for public pages (search, recommendations, etc.)
 */

export default function PublicLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
